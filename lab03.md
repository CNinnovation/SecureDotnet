# Lab 3

## XSS using a custom route

1. Create a custom route with an ASP.NET Core web application, return a string without encoding
2. Pass a HTML string
3. Pass a JavaScript code

> Try to run this malicious input with different browsers. What's different?

## XSS using a controller and view

1. Create the same issue was previously using an ASP.NET MVC controller and a view.

> How can you accomplish returning a string unencoded?

